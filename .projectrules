# .projectrules - BDD Agent Specific Patterns

**Inicializado**: 2025-08-27 16:19:05  
**Última Atualização**: 2025-08-27 16:45:42  
**Projeto**: BDD Agent - Sistema Python para geração de BDD com IA  
**Desenvolvedor**: Rafael Pelizza (PelizzAI)

---

## 🎯 **Padrões Específicos do Projeto BDD Agent**

### **Arquitetura e Estrutura**

#### **Organização Modular Descoberta**
```python
# Padrão: Separação clara entre apresentação, lógica e integração
src/
├── main.py                 # ✅ Entry point - Streamlit UI only
├── bdd_generator/         # ✅ Core business logic
│   ├── generator.py       # ✅ BDD orchestration
│   └── openai_client.py   # ✅ External API wrapper
├── [auth|config|ui|utils]/ # 📋 Placeholders bem organizados
```

#### **Responsabilidades Bem Definidas**
- **main.py**: APENAS interface Streamlit, delegação para core
- **generator.py**: APENAS orquestração BDD, não conhece UI
- **openai_client.py**: APENAS comunicação API, não conhece negócio

### **Padrões OpenAI Integration**

#### **API Key Validation Pattern**
```python
# Padrão específico descoberto: validação sk- + length
def _validate_api_key_format(self, api_key: str) -> bool:
    cleaned_key = api_key.strip()
    return cleaned_key.startswith('sk-') and len(cleaned_key) >= 20

# UI: Validação em tempo real + feedback visual
if cleaned_key.startswith('sk-') and len(cleaned_key) >= 20:
    st.success("✅ API Key configurada")
else:
    st.error("❌ Formato de API Key inválido")
```

#### **Model Configuration Strategy**
```python
# Padrão: Modelos hardcoded com metadata rico
SUPPORTED_MODELS = {
    "gpt-4o-mini": {"name": "GPT-4o Mini", "max_tokens": 4096, "cost_per_1k": 0.00015},
    "gpt-4.1-mini": {"name": "GPT-4.1 Mini", "max_tokens": 4096, "cost_per_1k": 0.0001},
    # Novos modelos incluindo versões futuras gpt-5-mini, gpt-4.1-nano
}
```

#### **Error Handling + Logging Pattern**
```python
# Padrão: Try-catch com logging estruturado + re-raise
try:
    response = self.client.chat.completions.create(...)
    logger.info(f"Tokens usados - Prompt: {usage.prompt_tokens}")
except Exception as e:
    logger.error(f"Erro na API OpenAI: {str(e)}")
    raise  # Always re-raise for caller handling
```

### **Streamlit UI Patterns**

#### **Layout Pattern Descoberto**
```python
# Padrão: Sidebar para config + main em colunas
with st.sidebar:
    # Configurações IA, validações
    
col1, col2 = st.columns([1, 1])
with col1:
    # Input do usuário
with col2:
    # Output/resultados
```

#### **State Management Pattern**
```python
# Padrão: session_state para configurações + validação condicional
config = {
    "incluir_negativos": st.session_state.get("incluir_negativos", True),
    "casos_extremos": st.session_state.get("casos_extremos", False),
    "num_cenarios": st.session_state.get("num_cenarios", 3),
}
```

#### **Feedback Visual Pattern**
```python
# Padrão: Loading + Success/Error com contexto
with st.spinner("🤖 Gerando cenários BDD..."):
    # Processing
    
if success:
    st.success("✅ Cenários gerados com sucesso!")
    st.markdown(result)
else:
    st.error(f"❌ Erro na geração: {str(e)}")
```

### **BDD-Specific Patterns**

#### **Gherkin Template Strategy**
```python
# Padrão: Template system prompt em português + user prompt dinâmico
system_prompt = """
Você é um especialista em BDD...
Regras importantes:
1. Use SEMPRE português brasileiro
2. Siga a estrutura: Funcionalidade, Cenário, Dado, Quando, Então, E
"""

user_prompt = f"""Gere {num_scenarios} cenários BDD para:
{user_story}
[Configurações dinâmicas baseadas em flags]
"""
```

#### **Scenario Classification Logic**
```python
# Padrão: Keyword-based classification
negative_keywords = ["erro", "inválido", "falha", "negativo", "incorreto"]
edge_keywords = ["limite", "extremo", "máximo", "mínimo", "vazio"]

# Busca em title + steps para classificação automática
```

#### **Parsing Strategy**
```python
# Padrão: Line-by-line parsing com state machine
current_scenario = None
current_steps = []

for line in lines:
    if line.startswith("Cenário:"):
        # Save previous, start new
    elif line.startswith(("Dado", "Quando", "Então", "E", "Mas")):
        current_steps.append(line)
```

### **Quality Patterns Específicos**

#### **Python Version Constraint**
```toml
# Padrão específico: Exclusão da versão 3.9.7
python = ">=3.9,<3.9.7 || >3.9.7,<4.0"
# Razão: Possível incompatibilidade conhecida
```

#### **Pydantic Model Pattern**
```python
# Padrão: Field descriptions em português para self-documentation
class BDDScenario(BaseModel):
    title: str = Field(..., description="Título do cenário")
    steps: List[str] = Field(..., description="Lista de passos do cenário")
    scenario_type: str = Field(default="positive", description="Tipo do cenário")
```

#### **Testing Strategy Pattern**
```python
# Padrão: Mock OpenAI com response structure específica
mock_client.chat.completions.create.return_value = Mock(
    choices=[Mock(message=Mock(content="Cenário BDD exemplo"))],
    usage=Mock(prompt_tokens=100, completion_tokens=200)
)
```

### **Naming and Documentation**

#### **File Naming Convention**
- **snake_case**: generator.py, openai_client.py, conftest.py
- **Descriptive**: Nomes que indicam responsabilidade clara

#### **Class/Method Naming**
- **Classes**: PascalCase + Descriptive (BDDGenerator, OpenAIClientWrapper)
- **Methods**: snake_case + verb_noun (generate_scenarios, create_completion)
- **Private**: Single underscore (_create_system_prompt, _parse_scenarios)

#### **Docstring Pattern (Google Style)**
```python
def generate_scenarios(
    self,
    user_story: str,
    num_scenarios: int = 3,
) -> BDDResponse:
    """Gera cenários BDD a partir de uma história do usuário.

    Args:
        user_story: História do usuário em linguagem natural
        num_scenarios: Número de cenários a gerar

    Returns:
        BDDResponse com os cenários gerados

    Raises:
        Exception: Se houver erro na chamada da API
    """
```

---

## 🚀 **Decisões Arquiteturais Específicas**

### **DA001 - Streamlit over FastAPI**
**Decisão**: Usar Streamlit para MVP ao invés de FastAPI  
**Razão**: Prototipagem rápida, foco em UX, menos código boilerplate  
**Trade-off**: Single-user sessions vs. multi-user scalability

### **DA002 - OpenAI over Local Models**
**Decisão**: Integração direta com OpenAI API  
**Razão**: Qualidade superior, menor complexidade de infra  
**Trade-off**: Dependência externa vs. controle total

### **DA003 - Pydantic for All Models**
**Decisão**: Usar Pydantic para toda validação de dados  
**Razão**: Type safety, documentação automática, JSON serialization  
**Impact**: Zero tolerance para dados não tipados

### **DA004 - No Database in MVP**
**Decisão**: Aplicação stateless sem persistência  
**Razão**: Simplicidade, deploy fácil, foco em core features  
**Future**: Database será adicionado em versões futuras

---

## 🔧 **Configurações Preferidas**

### **Development Workflow**
- **Poetry** para dependency management (não pip)
- **Black** line-length=88 (padrão do projeto)
- **MyPy** strict mode obrigatório
- **Pytest** com coverage ≥85%

### **Code Style Preferences**
- **Portuguese docstrings** para domínio específico
- **English code/variables** para padrões técnicos
- **Structured logging** com contexto
- **Exception re-raising** após logging

### **UI/UX Preferences**
- **Wide layout** Streamlit para aproveitar espaço
- **Emoji icons** para visual feedback (🧪, ✅, ❌, 🤖)
- **Real-time validation** ao invés de submit validation
- **Portuguese interface** para usuário brasileiro

---

## 📊 **Performance Patterns**

### **OpenAI API Optimization**
- **Timeout**: 30s default (não muito baixo, não muito alto)
- **Max tokens**: 2000 (suficiente para cenários BDD)
- **Temperature**: 0.3 default (criatividade controlada)

### **UI Response Patterns**
- **st.spinner** para operações >1s
- **Session state** para evitar re-computação
- **Caching strategy** planejada para futuro

---

## 🎯 **Evolution Guidelines**

### **When Adding Features**
1. **Manter separação** UI vs. Logic vs. Integration
2. **Usar Pydantic** para novos data models
3. **Documentar em português** aspectos de negócio
4. **Testar com mock** para componentes externos

### **When Refactoring**
1. **Preservar interfaces** públicas existentes
2. **Manter backward compatibility** quando possível
3. **Documentar breaking changes** no memory bank

### **Future Architecture Evolution**
1. **FastAPI backend** quando escalabilidade for necessária
2. **Database layer** quando persistência for crítica
3. **Auth system** quando multi-user for implementado
4. **Cache layer** quando performance for gargalo

---

## 🧪 **Padrões de Teste e Validação (Descobertos 2025-08-27)**

### **Testing Strategy Effective**
```python
# Padrão: Testes sistêmicos com múltiplas configurações
TESTING_PATTERN = {
    "domains": ["Admin", "Banking", "Gaming"],  # Diversidade de contextos
    "models": ["gpt-5-mini", "gpt-4.1-mini", "gpt-4o-mini"],  # Múltiplos modelos
    "configurations": {
        "creativity": [0.5, 0.6, 1.0],
        "scenarios": [1, 3, 5, 6],
        "types": ["positive", "negative", "edge_cases"]
    }
}
```

### **Model Configuration Evolution**
```python
# Padrão DESCOBERTO: Configuração dinâmica de modelos
MODELS_EVOLUTION = {
    "v0.1.0": ["gpt-4o-mini", "gpt-4-turbo", "gpt-3.5-turbo"],
    "v0.1.1": ["gpt-5-mini", "gpt-4.1-mini", "gpt-4o-mini"],  # ✅ Atual
    "pattern": "Sempre 3 modelos, com 1 padrão + 2 alternativos"
}

# ✅ REGRA: Sempre manter gpt-*o-mini como fallback confiável
# ✅ REGRA: Novo modelo mais avançado como padrão (gpt-5-mini)
# ✅ REGRA: Interface permite seleção fácil entre modelos
```

### **Download Format Strategy**
```python
# Padrão DESCOBERTO: Flexibilidade de formato baseado em contexto
DOWNLOAD_STRATEGY = {
    "v0.1.0": ".feature",  # Gherkin específico
    "v0.1.1": ".txt",      # ✅ Mais universal e flexível
    "reasoning": "Facilita integração com ferramentas diversas"
}
```

### **Error Handling Evolution**
```python
# Padrão DESCOBERTO: Debugging incremental durante testes
ERROR_PATTERNS_FIXED = {
    "api_key_passing": "BDDGenerator(api_key, model) vs BDDGenerator(wrapper)",
    "model_attributes": "scenario.title vs scenario.name alignment", 
    "download_format": "Consistent .txt generation",
    "lesson": "Testes abrangentes revelam inconsistências arquiteturais"
}
```

---

## 📊 **Métricas de Qualidade Estabelecidas**

### **Testing Quality Benchmarks**
```python
QUALITY_METRICS = {
    "functionality": 100,      # ✅ Todas features funcionando
    "usability": 95,          # ✅ Interface intuitiva
    "performance": 90,        # ✅ Tempos adequados
    "versatility": 100,       # ✅ Múltiplos domínios
    "robustness": 95,         # ✅ Error handling
    "overall_score": 97       # ✅ Excelência técnica
}
```

### **Validation Standards**
- **SEMPRE** testar múltiplos domínios (mínimo 3 diferentes)
- **SEMPRE** validar download functionality após mudanças
- **SEMPRE** testar todos os modelos disponíveis
- **SEMPRE** incluir cenários edge cases nos testes

---

**🎯 IMPORTANTE**: Estes padrões foram descobertos através da análise da base de código existente e testes extensivos realizados em 2025-08-27. Eles refletem as decisões e preferências já implementadas pelo desenvolvedor Rafael Pelizza, validadas através de testes práticos.
